extends layout

block content
  h1= "Looking for someone..."
  p Found:

  ul(class="nav nav-pills")
    each u in users
      li(class="my-nav-btn")
        a(href="#{u.href}") #{u.name}
    li(class="my-nav-btn my-nav-btn-back")
      a(href="/") Back to trains
